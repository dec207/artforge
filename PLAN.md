# 계획

> **1~3단계 보류 — devstory_d 전용이었다 (2026-08-19)**
>
> devstory_d가 스튜디오 밖으로 나갔다 (`studio_d/DECISIONS.md`). 1·2·3단계는
> 전부 그 게임의 픽셀 규격을 향해 있었으므로 대상을 잃었다.
>
> **artforge가 납품처를 잃은 것은 아니다.** 여기는 스튜디오의 모든 디자인과
> 에셋을 만드는 곳이고, 게임 아트는 그중 하나다. 다음 납품처는 홈페이지다 —
> `studio_d/design-system/BRAND.md`가 브랜드를 홈페이지에서 정한다고 했으니
> 로고·워드마크·사이트 비주얼이 곧 이 대장간의 일감이다.
>
> 오히려 제약이 풀렸다. 아래 "위험"과 2단계가 걱정한 것은 전부 하나였다 —
> 확산 모델 출력을 16×24나 40×40 실측 격자로 뭉개는 문제. 웹 해상도에는
> 그 격자가 없다. artforge가 가장 잘하는 크기에서 일하게 된다.
>
> 5단계도 앞당겨졌다. devstory_d가 없으니 마스코트 데뷔가 게임 1이다.

## 0단계 — 리그 세우기  ✅ 2026-08-18 완료

로컬 ComfyUI를 세웠다. 구성과 재설치 절차는 `SETUP.md`.

- ComfyUI 0.33.2, MPS, SDXL base 1.0 + pixel-art-xl LoRA
- `gen.py` — API 생성. PNG 옆에 파라미터 json을 남기고 `--replay`로 재현한다
- 실측 속도: 768/20스텝 83초, 1024/25스텝+LoRA 189초
- 두 경로 확인 완료 — 증거는 `samples/`

클라우드를 먼저 보려 했으나 로컬로 갔다. 결과가 나왔고 속도도 견딜 만하다.
탐색량이 커지면 그때 클라우드를 붙인다.

## 다음 단계 — 브랜드·웹 에셋  ◀ 지금 여기

납품처는 홈페이지다. `studio_d/design-system/BRAND.md`가 브랜드 색·서체·로고를
전부 미정으로 두고 "홈페이지를 만들면서 정한다"고 했다. 후보를 뽑는 일이 여기 있다.

- 로고 / 워드마크 후보 — studio_d가 고르고 확정한다 (경계는 `README.md`)
- 사이트 히어로·섹션 비주얼
- 브랜드 색 탐색 — 이미지에서 팔레트가 먼저 나오고 토큰이 따라온다

**픽셀 격자가 없다.** 1~3단계를 어렵게 만든 다운스케일 문제가 여기엔 없다.
SDXL이 원래 잘하는 해상도에서 그대로 쓴다.

납품은 PNG 그대로 간다. 텍스트 아트 변환기가 필요 없다 — 그것은 게임 쪽 사정이었다.

## 1단계 — 다리 놓기  ⏸ 보류 (devstory_d 전용)

변환기는 devstory_d에 만든다. 팔레트와 텍스트 아트 형식을 아는 곳이 거기다.

- `art-to-png` — 텍스트 아트 → PNG. 지금은 매 작업마다 임시 스크립트를 새로 짜고 있다
- `png-to-art` — PNG → 격자 다운스케일 → 70색 팔레트 스냅 → 텍스트 아트

**팔레트 스냅은 OKLab 최근접으로 한다.** RGB 최근접으로 하면 살결이 진흙이 된다.
이것이 파이프라인의 핵심이다.

**왕복 항등 테스트**로 잠근다 — 기존 `HEADS`·`BODIES`를 PNG로 내보냈다 되읽으면
원본 문자열과 바이트 단위로 같아야 한다.

## 2단계 — 파일럿 1건 (게이트)  ⏸ 보류 (devstory_d 전용)

대상: devstory_d의 40×40 직업 일러스트 8종 (`src/data/sprites/jobArts.ts`).

이유 — 16×24보다 크다. 원작 참조가 약해 현재 자체 창작이다.
실패해도 실측으로 채운 액터 스프라이트를 건드리지 않는다.

**판정**: 지금 손으로 찍은 8종과 나란히 놓고 본다.
이기면 3·4단계. 지면 artforge를 콘셉트 전용으로 줄인다.

**솔직한 승률은 반반이다.** 확산 모델이 실측 픽셀을 이기기 어렵다.
여기 며칠 쓰는 것은 게임에서 빠지는 며칠이다.

## 3단계 — 캐릭터 로스터 설계  ⏸ 보류 (devstory_d 전용)

치비 시트를 대량 생성해 devstory_d의 머리 8종 / 몸 8종 / 액세서리 4종을 정한다.
현재 이 24종은 근거 없이 정해진 것이다. 직원 44명의 외형 조합표까지 여기서 확정한다.

최종 픽셀은 손으로 찍는다.

## 4단계 — 큰 배경

타이틀 스카이라인, 사무실 창밖 거리, 잡지 표지. 240폭이면 다운스케일이 잘 먹는다.

## 5단계 — 마스코트

`studio_d/design-system/WORLD.md`가 마스코트를 두기로 정하면 후보를 여기서 뽑는다.
확정은 거기서 한다.

데뷔는 게임 1이다. 원작 충실 재현이라 마스코트를 못 받던 devstory_d가
빠지면서 제약이 풀렸다 — 첫 게임이 처음부터 마스코트를 안고 시작한다.

## 위험

가장 큰 위험은 리그를 세워놓고 결과물을 안 쓰는 것이다.

**이 위험은 devstory_d 한정이었다.** 원작 실기기 캡처 44장이라는 더 강한 소스가
있었고, 확산 모델이 그 실측을 이기기 어려웠다. 2단계를 게이트로 둔 이유다.
그 대상이 나갔으니 이 형태의 위험도 같이 나갔다.

브랜드·웹 에셋에는 경쟁하는 소스가 없다. 로고도 사이트 비주얼도 아직 백지다.
여기서는 안 쓰일 걱정이 아니라 **studio_d가 고르지 않으면 아무것도 확정되지
않는다**는 것이 위험이다. 후보를 쌓아두고 결정을 미루지 않는다.
